# this is a file for the global


# loading in packages -----------------------------------------------------
library(shiny)
library(reactable)

# loading in data ---------------------------------------------------------
table_data <- data.frame(
  number = c(),
  links = c(),
  description = c()
)

# utility functions -------------------------------------------------------


